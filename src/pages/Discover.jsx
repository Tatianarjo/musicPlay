// import { Error, Loader, Songcard } from "../components";
import { genres } from '../assets/constants';

const Discover = () => {
  console.log(genres);

  return <div className="flex flex-col">Discover</div>;
};

export default Discover;